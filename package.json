{
  "name": "truyen-gg-clone",
  "version": "1.0.0",
  "description": "Website đọc truyện tranh - Frontend React + Backend Node.js Express + MySQL",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["react", "nodejs", "express", "mysql", "comic"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

